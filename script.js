// const slider = document.querySelector('.breaking-slider');
// const items = document.querySelectorAll('.breaking-item');
// let currentIndex = 0;

// function slideNews() {
//   currentIndex++;
//   if (currentIndex >= items.length) {
//     currentIndex = 0; // Reset to first item
//   }
//   const offset = -currentIndex * items[0].offsetWidth; // Calculate offset
//   slider.style.transform = `translateX(${offset}px)`; // Move slider
// }

// // Slide every 3 seconds
// setInterval(slideNews, 3000);